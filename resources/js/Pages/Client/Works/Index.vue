<script setup>
import ClientLayout from '@/Layouts/ClientLayout.vue';
defineProps({ works: Object });
</script>
<template><ClientLayout title="Lucrarile mele"><div class="mx-auto max-w-7xl px-4 py-8 sm:px-6 lg:px-8"><h1 class="text-2xl font-bold text-slate-900">Lucrari si interventii</h1><div class="mt-6 grid gap-4"><div v-for="work in works.data" :key="work.id" class="rounded-xl bg-white p-6 shadow-sm"><div class="flex flex-wrap justify-between gap-3"><div><h2 class="font-semibold text-slate-900">{{ work.type || 'Interventie tehnica' }}</h2><p class="mt-1 text-sm text-slate-500">{{ work.address || 'Adresa nespecificata' }}</p></div><span class="rounded-full bg-blue-50 px-3 py-1 text-xs text-blue-700">{{ work.status }}</span></div><div class="mt-4 grid gap-2 text-sm text-slate-600 sm:grid-cols-3"><div><b>Programare:</b> {{ work.scheduled_at || '-' }}</div><div><b>Tehnician:</b> {{ work.technician?.name || 'Neasignat' }}</div><div><b>Finalizat:</b> {{ work.completed_at || '-' }}</div></div><p v-if="work.customer_notes" class="mt-4 rounded bg-slate-50 p-3 text-sm text-slate-600">{{ work.customer_notes }}</p></div><p v-if="!works.data.length" class="rounded-xl bg-white p-6 text-slate-500">Nu exista lucrari inregistrate.</p></div></div></ClientLayout></template>
